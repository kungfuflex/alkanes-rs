// Build script removed - WASM files are now pre-built using ./scripts/build-std.sh
// The generated WASM files are committed to the repository in ./src/tests/std/wasm/
// To regenerate the WASM files, run: ./scripts/build-std.sh from the repository root

fn main() {
    // No build steps needed - all WASM files are pre-built
}
