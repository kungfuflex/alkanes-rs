[package]
name = "memshrew-runtime"
version.workspace = true
edition = "2021"

[dependencies]
metashrew-runtime = { path = "../metashrew-runtime" }
metashrew-core = { path = "../metashrew-core" }
anyhow = "1.0.86"
log = "0.4.22"
metashrew-sync = { path = "../metashrew-sync" }
async-trait = "0.1.83"

[dev-dependencies]
tempfile = "3.8.1"

[features]
native-deps = []

