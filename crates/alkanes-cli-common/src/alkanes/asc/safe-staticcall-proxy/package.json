{
  "name": "safe-staticcall-proxy",
  "version": "0.1.0",
  "description": "Alkane that proxies staticcalls to catch reverts gracefully",
  "scripts": {
    "asbuild:release": "asc assembly/index.ts --target release --exportStart _start"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.29"
  }
}
