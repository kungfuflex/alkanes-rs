#[cfg(test)]
mod tests {
    #[allow(unused_imports)]
    use metashrew_core::{
        index_pointer::IndexPointer,
        println,
        stdio::{stdout, Write},
    };

    /*
    #[wasm_bindgen_test]
    fn test_response_serialization() -> Result<()> {
        clear();
        Ok(())
    }
    */
}
